#ifndef COLA_H
#define COLA_H
#include "common.h"
/* Registra el arribo en una cola de espera (cola.csv) asignando posición por orden de llegada. */
void cola_sidecar_log(const registro_t* r);

#endif
